import { PreventUnwantedClicksDirective } from './prevent-unwanted-clicks.directive';

describe('PreventUnwantedClicksDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventUnwantedClicksDirective();
    expect(directive).toBeTruthy();
  });
});
